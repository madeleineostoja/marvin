{
  "name": "marvin",
  "version": "0.0.0",
  "description": "Autonomously writing code so you don't have to. Not that anyone ever says thank you.",
  "license": "MIT",
  "bin": {
    "marvin": "./dist/index.js"
  },
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node ./index.ts",
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit",
    "prepare": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@anthropic-ai/sandbox-runtime": "^0.0.34",
    "execa": "^9.6.1",
    "string-width": "^8.1.1",
    "wrap-ansi": "^9.0.2",
    "yocto-spinner": "^1.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "typescript": "^5.9.3"
  }
}
